#Raycast
stow -t $HOME/.config/raycast -D raycast
stow -t $HOME/.config/raycast -v raycast

#ZSH
stow -t $HOME -D zsh
stow -t $HOME -v zsh

#Zed
stow -t $HOME/.config/zed -D zed
stow -t $HOME/.config/zed -v zed
